/** `baselayer learn` — terminal course player (roadmap placeholder). */
function learn() {
  console.log('Terminal course player is on the roadmap.');
  console.log('For now: run `baselayer up` and take courses in the browser studio.');
  console.log('It will read the same Topic/Explanation/Example/Assignment lessons,');
  console.log('run micro-steps through the local sandbox, and sync progress to MEMORY.md.');
}

module.exports = { learn };
