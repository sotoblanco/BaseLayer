# Matrix Dot Product — Connect the Lines

In matrix multiplication, **every cell in C is the dot product of a row from A and a column from B**.

For **1×1 matrices**, this is simply:

> **C = A · B**

---

## Your Task

Look at the diagram on the right. You can see:
- **A = [2]** — a pink box on the left
- **B = [5]** — a yellow box on the top
- **C = [?]** — an empty blue box on the right

Using the **pencil tool**, draw:

1. An **arrow from A to C** (showing that A contributes to C)
2. An **arrow from B to C** (showing that B contributes to C)

This represents the data flow of the dot product: both A and B "flow into" the result C.

> **Hint:** The bottom diagram shows the expected connection pattern — A points right into C, and B points down into C.

When you're done drawing, click **Submit Drawing** ✏️
